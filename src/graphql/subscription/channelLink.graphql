subscription ChannelRepoLink {
  ChannelLink {
    channel @required {
      channelId
      name
    }
    repo @required {
      owner
      name
      defaultBranch
      org {
        provider {
          apiUrl
          url
        }
        chatTeam {
          id
        }
      }
    }
  }
}
